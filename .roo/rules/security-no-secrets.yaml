id: security/no-secrets
level: error
description: "Prevent secrets from being committed or printed to logs"
files:
  include:
    - "**/*"
detector: secret-regex
options:
  patterns:
    - "AWS_ACCESS_KEY"
    - "PRIVATE_KEY"
fix:
  type: templateRef
  path: docs/templates/secret-ignore.md
