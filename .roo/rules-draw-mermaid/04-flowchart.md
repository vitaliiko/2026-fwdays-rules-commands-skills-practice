# Flowchart Best Practices

- Direction must be explicit (`TD`, `LR`, `TB`).
- Use shapes consistently:
  - Rectangle: process
  - Diamond: decision
  - Rounded: start/end
- Label decision branches clearly (`Yes`, `No`).
